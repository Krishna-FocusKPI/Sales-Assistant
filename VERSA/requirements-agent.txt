# Primary dependency set for VERSA (LangGraph / LangChain 0.2+). Used by Dockerfile and DigitalOcean App Platform.
# Local install: pip install -r requirements-agent.txt
# Legacy LangChain 0.1 stack (conflicts with this file): see requirements.txt — do not install both in one venv.
langchain>=0.2
langchain-core>=0.2
langchain-openai>=0.2
langchain-anthropic>=0.2
langgraph>=0.2
streamlit>=1.37
tqdm>=4.66
graphviz>=0.20
python-pptx>=0.6
pandasql>=0.7
tabulate>=0.9
snowflake-connector-python>=3.10
gnews>=0.3
beautifulsoup4>=4.12
fake-useragent>=1.5
