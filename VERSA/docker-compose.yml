services:
  versa:
    build: .
    image: versa:latest
    container_name: versa
    environment:
      - VERSA_BASE_URL_PATH=/salesbuilder
    volumes:
      - /usr/share/nginx/html/:/downloads
      - config:/.versa
    network_mode: host
    restart: on-failure

volumes:
  config:
