services:
  versa:
    build: .
    image: versa:latest
    container_name: versa
    volumes:
      - /usr/share/nginx/html/:/downloads
      - config:/.versa
    network_mode: host
    restart: on-failure

volumes:
  config:
